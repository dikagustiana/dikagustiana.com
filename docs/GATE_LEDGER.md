# GATE LEDGER

The primary artefact for the gated-completion mandate. One row per gate.

**States**

- `PASSED` — observed on running software, with the measurement recorded below.
- `CODED-NOT-VERIFIED` — the code exists; the gate was not run. Honest and acceptable; a
  session may end here. Pick it up cleanly next time.
- `BLOCKED` — with the specific reason and what would unblock it.

**A gate is never passed by** `tsc --noEmit`, a green build, passing unit tests, code that
looks correct, or reasoning that it should work. All five were true of `getEssayLink` while
it returned a route that does not exist.

| # | Gate | State | Evidence |
|---|------|-------|----------|
| 0 | `main` contains Phase A+B; `_pending` drop still unapplied | **PASSED** | PR #5 merged as `1d6c7f4`. Read `src/lib/presentation.ts` back from `refs/heads/main` via the GitHub API — file present, blob `1641e99`. Live DB `information_schema.columns` for `public.essays` returns all four of `manager_fields`, `economist_fields`, `educator_fields`, `coach_fields` plus `presentation` → drop not applied. |
| 0b | Two merged branches deleted | **BLOCKED** | `claude/dikagustiana-supabase-rebuild-ioiyd1` and `claude/dikagustiana-supabase-phase-2-w564rz` both have 0 unique commits vs `main` (`git rev-list --count`), so deletion is safe. The git proxy rejects delete-pushes (`send-pack: unexpected disconnect`, 3 attempts) and the GitHub MCP exposes no delete-branch tool. **Unblocked by:** deleting both in the GitHub UI. `archive/pre-rebuild-history` deliberately untouched (417 unique commits, only copy of pre-rebuild history). |
| 1a | Every homepage/About card resolves; no dead `/essays/` links | **PASSED** | Browser sweep (`gate1a.mjs`) against the live DB. Collected all internal `<a href>` on `/` (24) and `/about` (16), deduped to 14, navigated to each and classified the *rendered* page (an SPA always returns HTTP 200, so the catch-all's NotFound render is the real signal). **0 of 16 targets rendered 404.** Key observations: the homepage card now emits `/finance/analytics/t4-m07/fa-07-01` — previously `/essays/fa-07-01`, which matched no route; `/essays/fa-07-01` **redirects** to the canonical URL (final path differs, 23,428 chars rendered); `/essays/site-rebuild-note` now renders (2,420 chars) where it previously had no working URL anywhere. About's essay section legitimately renders nothing — `is_selected` is true on 0 rows (verified in SQL), an intentional empty state, not a break. |
| 1b | Curriculum index shows truthful planned-vs-published; anon still sees 0 of 160 drafts | **PASSED** | Both halves measured, and the fix does not leak drafts. **Display:** `/finance/analytics` as a genuine anonymous visitor (0 localStorage keys) renders `0 of 2 published`, `1 of 3 published`, … — no `0 essays` string remains. **Security:** anon `GET /essays` → `content-range: 0-0/2`; anon `GET /essays?published=eq.false` → `content-range: */0`. Zero of the 160 drafts are visible, and no draft title appears on the page. **How:** the planned count moved to `finance_modules.module_meta.essay_count` (anon-readable), populated from the current stub counts by migration `20260801100000`, whose own assertion checks the total equals the 161 placed stubs. `useTrackEssayCounts` — the hook actually rendering the index — now takes `total` from that meta and counts only published rows. **Second defect fixed:** `useModuleLessonCounts` ran 1 + 2N sequential queries (99 requests for a 49-module track); it is now two. |
| 1c | Staged drop migration rehearsed on scratch DB; admin preview still renders; then applied live | **PASSED** | **Rehearsed:** built a scratch Postgres replica (raw `initdb`/`pg_ctl` on :5433 — never `supabase db reset`, which the mandate forbids), applied the harness + all 10 migrations, seeded `fa-07-01`'s real payload from the content dump, then ran the staged drop. All four columns dropped; `presentation` survived with 5 references / 3 key takeaways / 140-char deck. The scratch run also proved the migration's own guard works — it correctly refused an earlier attempt when `fa-07-01` had no payload. **Ordering discovered and respected:** selecting a dropped column is a hard PostgREST error, not a silent null, so every `economist_fields` select had to be removed *before* the drop, not after. **Verified pre-apply** with the app already running post-drop-shaped: public page rendered deck + all 5 references + takeaways with 0 bad HTTP statuses; admin editor rendered deck + references + takeaways with 0 bad statuses. **Applied live**, then **re-verified — byte-identical results** (public 23,428 chars, admin editor 44,732). Live now: 0 legacy columns, `fa-07-01` 5 refs / 3 takeaways / 140-char deck, 0 tables without a policy. Migration moved out of `_pending`. |
| 1d | List-view payload measured in bytes, before and after | **PASSED** | Measured against the live REST API with an admin token, same rows both times. **Published-essay list:** `select=*` → **58,043 bytes**; card columns → **1,116 bytes** (−98.1%). **Admin list, all 162 rows:** `select=*` → **219,390 bytes**; card columns → **84,164 bytes** (−61.6%). The published list collapses so far because `fa-07-01` alone drags ~22k of HTML plus its JSON document. Applied via `ESSAY_CARD_COLUMNS` in `useEssays` (list, by-FSLI, by-topic) and `useAdminEssays`. Detail pages keep their own body-bearing selects. |
| 1e | View-live button opens the correct canonical URL for `fa-07-01` | **PASSED** | Re-run from a cold dev server (the earlier non-reproduction was a Vite HMR artifact). `/admin/writer/finance/list` as admin: View-live anchors are `/finance`, **`/finance/analytics/t4-m07/fa-07-01`**, `/essays/site-rebuild-note`. The canonical four-segment URL is present and built by `essayUrl`. `site-rebuild-note`, which has no placement, correctly gets the universal route rather than a fabricated one. |
| 1f | Wrong essay-shaped URL produces the improved 404 | **PASSED** | Root cause fixed: all four essay pages did `return <Navigate to="/<section>" replace />` on their local `notFound` state, silently teleporting the reader to the section index instead of saying the URL was wrong — and that ran before the `!essay` check, making the improved 404 unreachable. All four now render `<NotFound />`. Observed: `/finance/analytics/t4-m07/fa-07-01-TYPO` → *"404 Page not found … Return home / Browse this track"*; `/finance-101/fa-07-01` (unmatched route, real slug) → *"404 … DID YOU MEAN Driver Tree Construction"*. **Finding, not a break:** `/finance/wrongtrack/wrongmodule/fa-07-01` still renders the essay, because the page looks up by globally-unique slug and never checks that track/module match. Duplicate-URL/canonicalisation issue, logged for Section 4. |
| 2 | Both reports in `DECISIONS.md`; takeaways finding targets `WriterValidation.tsx` | **PASSED** | Both written in `docs/DECISIONS.md`. The key-takeaways finding cites `src/components/writer/WriterValidation.tsx:54-60` and quotes the actual `filledTakeaways < 3` check, explicitly noting it is *not* in the deleted `publishValidation.ts` (dead code imported only by its own test). Recommends scoping per `lesson_type` rather than relaxing globally. The Topic/Phase report is grounded in live counts: `topic` NULL on **all 162** rows, `phase` set on **1**, `module_id` on **161** — and records that `WriterMetadata.tsx:162` labels the control required while `validateEssay` never checks it, so the asterisk is decorative. Recommends deriving both from module placement. |
| 3 | Font computed (not fallback); 5 pages at 375px with no h-scroll; 44px tap targets | **PASSED** (ui-audit not run) | **Font — actually loading, not falling back.** Plus Jakarta Sans self-hosted (two variable woff2 subsets, 56K total) rather than `@import`ed, because a webfont that fails to load falls back silently. Applied at the token layer: `--font-sans` / `--font-display` in `:root`, consumed by `tailwind.config.ts` via `var(...)`, so no component names a family. Measured in-browser after `document.fonts.ready`: computed `font-family` on body and paragraphs is `"Plus Jakarta Sans", ui-sans-serif, …`, `document.fonts.check('16px "Plus Jakarta Sans"')` → **true**, latin face status **loaded** (latin-ext correctly stays unloaded — no such glyphs on the page). **375px — 0 of 9 pages scroll horizontally** (homepage, track index, module index, essay, admin writer, consolidation, books category, book reader, tracker archive; all `scrollWidth == 375`). Two root causes fixed, not nine symptoms: the breadcrumb trail did not wrap (five routes) and the writer-list header row did not wrap. `/green-transition/tracker/archive` needed no change — its tab nav already scrolls inside its own container. **Tap targets: 0 under 44px** on the header primary nav across four pages, and 0 of 15 in the opened mobile menu (hamburger 40→44, logo 32→44, submenu chevrons 40→44, Sign In 21→44, sheet Close 16→44). **`ui-audit` not run** — recorded as outstanding rather than claimed. |
| 4 | All routes have a verdict; zero `NOT TESTED` | **PASSED** | `docs/ROUTE_VERIFICATION.md`. **65** routes, three identities, two viewports. Final run: **0 defects, 0 access leaks, 0 horizontal-scroll, 0 NOT TESTED.** Gating verified by separation: `/admin/content` renders 507 chars anon / 1,941 non-admin / **39,759** admin. **Three harness defects found and fixed** (each produced a confident wrong answer): the login helper reported success with no session, so a failed login emitted a full pass of anonymous results labelled `admin`; the blank-screen check ran before the 404 check and flagged four working 404s; and a 1.5s probe wait reported `/admin/writer/:id` as blank when it renders 21,745 chars in ~4s. **Four product defects found and fixed:** PostgREST 406s behind pages that rendered fine (`.single()` on zero rows) across four routes. |
| 5 | Image round-trips to anon read; non-admin refused; all block types survive; preview == published; one authoring route; no duplicate-extension warning | **PASSED** | All eight requirements observed by the parallel session at `c7e5d12` and re-verified on the merged tree at `5bdc5e4` — full evidence in `docs/gates/GATES_5_7.md`. Merged into `main` via PR #9 (`a1c2e50`), verified by reading `src/lib/tiptap/imageUpload.ts` back from `refs/heads/main`. Headlines: pasted image survives save → reload → publish → anonymous read decoding at 160×100; non-admin upload refused with 403 RLS (both anonymous and authenticated); table insert had crashed the page past typecheck AND build (`insertBefore` reconciliation error) — the exact bug class the gates hunt. |
| 6 | 3,000-word reload recovery re-observed; dump script run and inspected | **PASSED** | **Dump half PASSED.** `npm run dump:content` (`scripts/dump-content.mjs`) run once against the live project with an admin JWT and the output inspected: 162 essays (all 160 drafts included), 2 revisions, 49 modules, 24 FSLI pages, 384K. `fa-07-01` present with its **21,946-character body**, **81 `content_json` blocks**, and a presentation payload carrying **5 references, 3 key takeaways and the 140-character deck**. The script refuses to write a false sense of safety: it requires a service-role key or admin JWT (an anon dump would 'succeed' with the drafts hidden) and exits non-zero if any table falls below an expected row floor. **Autosave half now PASSED (2026-08-01, Fable session).** 3,200 generated words pasted into the real stub `fa-01-01` on the built preview; "Backed up" indicator observed; hard reload mid-edit; "Unsaved draft recovered from backup" banner appeared; Restore clicked; **3,200 of 3,200 words recovered** — counted in the editor, not asserted. Note this ran *after* the orphaned tone-fields trigger was dropped (see F.3 below): before that fix the recovery could never have been saved anyway, because every essays UPDATE errored. **Independently re-observed (PR #11 session, W1):** 3,612 words pasted into the real stub `sf-01-04` through the dev server against the live project, hard reload with no manual save, recovery banner offered, Restore returned every word including an edit made after the first backup — a second, separate pass of the same gate after that branch's save-path changes. |
| 7 | Each of the five never-checked module groups has a verdict | **PASSED** | Verdicts written and observed by the parallel session — the full table, in Gate 4's column format, is in `docs/gates/GATES_5_7.md` (GATE 7, commit `b589d9a`), merged via PR #9. All five groups verdicted across three identities plus 375px: curriculum admin **working**; FSLI **working but boilerplate** (now made honest — see F.2 below); books and models **half-built, no insert path** (books path now built — F.1 below); three swallowed PostgREST 406s fixed. Superseded facts kept for the record: data facts established against the live DB were: `fsli_pages` **24 rows** but `fsli_sections` **0** — the FSLI pages exist with no section prose. `books_uploads` **0**. `finance_models` **0** (this is curriculum Section 06's territory, so Section 06 has no data path, as suspected). `finance_settings` 2. Curriculum **Section 05 = `capital-allocation`**, which exists as a `finance_sections` row with **zero modules** (modules per track: fundamentals 20, strategic-finance 13, analytics 9, planning 7 = 49). All five groups' routes render without defects anonymously (see `ROUTE_VERIFICATION.md`), but 'renders an empty state' is not the same as 'works', and the admin surfaces are exactly the ones the unverified identity dimension would have covered. |

## Fable session (2026-08-01) — reconcile + make every title reach its essay

Measured on `vite build` + `vite preview` (`127.0.0.1:4173`) against the live project,
Playwright/Chromium, Supabase traffic relayed through Node (Chromium in this container
cannot complete TLS to `*.supabase.co`; the page makes its real calls with real
responses — only the transport hop differs; TLS verification never disabled).

| # | Gate | State | Evidence |
|---|------|-------|----------|
| A | `main` contains the merged work; typecheck/build/tests; `/books/:category` at 375px measured | **PASSED** | The merge had already landed as **PR #9 → `a1c2e50`** before this session began. Read-back: `src/lib/tiptap/imageUpload.ts` fetched from `refs/heads/main` (blob `28ddf70`). `npm run typecheck` (`tsc -b --force`) exit 0 — **247** src files genuinely in the app project's compilation (`--listFilesOnly`; the mandate's 249 was counted on the pre-merge tree, before that branch's 5 deletions / 3 additions). Build green; **165/165** unit tests pass. **`/books/:category` at 375px: `scrollWidth` 375 / `clientWidth` 375 — no horizontal scroll.** The stale-branch report about `Breadcrumb.tsx` was indeed stale: the merged file wraps (`flex-wrap`/`min-w-0`/`break-words` with Session A's comment). **`.single()` union verified:** the union of both sessions' fixes is on `main` (`useFinance` track lookup + `useFsliPages` + `useBooks` + `useFinanceModels`, plus `WriterEditor`'s loader already on `maybeSingle`); no site reverted; the survivors are all insert/update-returning (a write returns exactly one row) except one **dead-code** lookup in `useWriterEssay` (zero callers), converted anyway. **ROUTE CHANGES REQUESTED dispositions:** (1) `/admin/writer/:id` route **kept** — `WriterStudio` is a redirect that keeps `/admin/writer/new` (used by AdminDashboard) and old bookmarks resolving at zero cost; the one-authoring-surface requirement is already met; (2) no Section 7 route removals — reaffirmed; (3) breadcrumb `flex-wrap` — already fixed by Session A, verified at 375px above. **Branch deletions: BLOCKED** — the git proxy has rejected delete-pushes across two sessions and the GitHub MCP still exposes no delete-branch tool; the five zero-unique-commit branches (`council-removal-persona-decouple-x7k2m`, `dikagustiana-supabase-phase-2-w564rz`, `dikagustiana-supabase-rebuild-ioiyd1`, `gated-completion-sweep-m3p9x`, `route-sweep-s4`) need the GitHub UI. `archive/pre-rebuild-history` untouched. |
| B | Route count reconciled; autosave word count recorded; ui-audit | **PASSED** (ui-audit substituted, see below) | **B.1** — the unaccounted route is the **`path="*"` catch-all**: `App.tsx` has 66 `path=` entries, the sweep documented 65 and *explicitly excluded* the catch-all from its count while exercising it in every 404 test. Now swept in its own right: `/definitely-no-such-route-xyz` renders the improved 404 as **anonymous, non-admin and admin**, at 1440 and 375px, `hScroll false` everywhere. `ROUTE_VERIFICATION.md` summary corrected to 66 with a catch-all row. **B.2** — **3,200 of 3,200 pasted words recovered** after a hard reload mid-edit on the real stub `fa-01-01` (banner → Restore → counted in the editor). **B.3** — the `ui-audit` skill **does not exist in this environment** (skill listing checked on disk and via skill search; only `component-craft` is installed). Substituted with a manual consistency sweep over the surfaces this session touched, recorded in the Notes below including what was deliberately not fixed. |
| C | Every essay title links canonically or deliberately doesn't; mismatched URLs heal; Draft label reflects data | **PASSED** | **C.1** — `FinanceTrackIndex` essay rows are now `<Link>`s through the canonical `essayUrl`; observed: clicking the published row lands on `/finance/analytics/t4-m07/fa-07-01`. **C.2** — the hardcoded `Draft · Dika Gustiana` is gone; observed bylines: `Published · Dika Gustiana · 16 min read` on the published essay, `Draft · Dika Gustiana` on stubs — read from `status`/`author`, not a literal. **C.3 sweep** — every essay-title renderer classified: *canonical builder* (Index, About, WriterList, AdminContent, NotFound's did-you-mean, EssayCardLink consumers, RelatedEssays and RelatedContent — the last two **fixed this session**: their private pre-canonical builders emitted `/finance/<slug>`, which is the TRACK route, for finance essays); *canonical-shaped section literals* (FinanceModulePage, GreenTransition\*, ClimateFinance, CriticalThinkingPhase, DevelopmentFinance\*, EssayModule, EssayNavigation via page-supplied builders); *deliberately not links* (FsliDetail's linked essays — full bodies rendered in place on their canonical page; FinanceWorkspace's featured-essay picker; EssayDialog's in-place modal; WriterMetadata form fields). **AdminDashboard fixed**: both recent-essay lists linked to `/admin/writer/<slug>` — the retired studio route, which redirected a slug to a nonsense section list; now `/admin/writer/<section>/<slug>`, observed rendering 7 correct editor links. **Clicked:** homepage card → canonical fa-07-01; track index published row → canonical; track index draft row → editor (admin); module page rows (already linked, re-observed); Continue Reading card → `/essays/site-rebuild-note` (universal route for the unplaced essay — correct); admin content/writer list URLs verified canonical in GATE 1e. **C.4** — `/finance/wrongtrack/wrongmodule/fa-07-01` and `/finance/analytics/WRONGMODULE/fa-07-01` both **redirect to the canonical URL** (observed, essay rendered); placement is validated against the essay's joined module and an unplaced essay's fabricated four-segment URL redirects to `/essays/:slug`. Redirect, not 404, matching `/essays/:slug`'s existing healing behaviour — the reader asked for a real essay at a wrong address. |
| D | Admin edit affordance; empty-stub editor works and saves; drafts invisible with `content-range: */0` measured | **PASSED** | **D.1** — `EssayEditLink` pencil beside titles on the track index and module page rows, sibling of the row link (never a nested anchor); **5 pencils observed as admin, 0 as non-admin, 0 anonymous**. For a draft, the title itself routes to the editor ("Edit is the more useful default" — a stub's public view is an empty page). **D.2** — real empty stub `fa-02-01` (0-byte body, NULL json) opened in a **working editor** (real title rendered, no boundary, no blank shell), a sentence typed, **Save Draft → "Saved!"** — which is also the end-to-end proof of the F.3 trigger fix. **D.3** — drafts query with `Prefer: count=exact`: **`content-range: */0` as anonymous AND as authenticated non-admin**, measured at the REST layer. Non-admin also observed with 0 draft rows and 0 pencils on the track index, and bounced off `/admin/writer/...`. |
| E | Sticky ToC sidebar tracking to the last heading; 375px clean; reduced-motion jumps | **PASSED** | Desktop (1440): ToC renders in a **sticky aside** (`ArticleShell` two-column grid at `lg:`; `LongformArticleShell` given the same treatment plus the inline mobile ToC it never had), **13 entries**, list `overflow-y: auto` with `max-h` so a short viewport cannot truncate the tail unreachably. Scrolled to the end of `fa-07-01`: active entry = **"Least-confident assertions", `isLastEntry: true`, `visibleInList: true`** — the active-entry scroll happens *inside the list container* via direct `scrollTop` adjustment, so tracking it can never scroll the page. 375px: `scrollWidth` **375/375** on the essay page, sidebar absent, inline collapsible present. **Reduced motion:** first observation FAILED — the jump was still animating 120ms after the click. Root cause: `index.css` sets `scroll-behavior: smooth` on `html`, and per spec `scrollIntoView({behavior:'auto'})` **defers to CSS**, so the "instant" branch quietly re-animated. Fixed twice over: the CSS is now inside `@media (prefers-reduced-motion: no-preference)` and the JS branch uses `'instant'`. Re-observed with `reducedMotion: 'reduce'` emulated: a **10,433px jump completed within 120ms** (landed 80px short = the heading's scroll-margin). `ReadingProgress` scroll handler wrapped in `requestAnimationFrame` (one `scrollHeight` read per painted frame). |
| F | Four leftover defects — verdicts | **PASSED** | **F.3 — the big one. Every essay save in production was dead.** `validate_essay_tone_fields` still referenced all four dropped `*_fields` columns; plpgsql resolves `NEW.<field>` when the expression is planned, so since the column drop **every INSERT/UPDATE on `essays` raised `42703: record "new" has no field "manager_fields"`** — reproduced with a no-op UPDATE before the fix. Save Draft, Publish, essay creation: all broken while every read looked fine; nobody had re-run a save after the drop. The trigger's other branch was the `tone_pending` status rewrite GATE 5 observed. Nothing in it was worth preserving (persona system deleted; the publish gate lives in `WriterValidation`): **dropped trigger + function via `apply_migration` (`drop_orphaned_tone_fields_trigger`)**, re-ran the failing statement — draft stays `draft`, published stays `published` — then proved it end-to-end with D.2's in-app save. **F.1** — `books_uploads`: **insert path built** (`useUploadBook` + admin-only `BookUploadCard` on `/books/:category`; storage + table writes are admin-only by RLS). Observed: PDF uploaded through the UI as admin → "Book added" → listed → reader renders title + download, no error state; upload card invisible to non-admins. Test row and object deleted after observation. `finance_models`: **written decision, not built** — the mandate itself excludes Section 06's data path from this session; surfaces kept because read/render/empty-state/edit paths all work and the 11 institutional models are the owner's framework content; recorded in `DECISIONS.md` so no third session rediscovers it. **F.2** — FSLI honesty: the cash-equivalents placeholder prose deleted at the source, `FsliContentSection` renders *"This section hasn't been written yet."* (with an admin click-to-write affordance), edit seeds with actual content only, the hardcoded key-points trio and fake *"Updated 6 Sep 2025 · 6 min read"* replaced with the row's real `updated_at`. Observed on `advances-prepayments-current` AND `cash-and-cash-equivalents`: **0 cash prose, 10 honest empty-state markers each, no fake metadata**. Dead duplicate `FsliSection.tsx` (same placeholder-seeding bug, zero imports) deleted. **F.4** — already resolved by the merged branch: the five StarterKit-bundled `@tiptap/extension-*` packages are absent from the merged `package.json` (verified by grep — only image, table, suggestion, drag-handle-react, placeholder, react, starter-kit remain). |
| G | Recorded decisions applied and observed | **PASSED** | **G.1** — takeaways gate scoped per `lesson_type` exactly as the decision on record specifies. Observed live in the editor with no takeaways set: `concept` → blocking error shown; flipped to `exercise` → **error gone, advisory warning shown** ("No key takeaways — fine for a exercise…"); typed 1 of 3 → **all-or-nothing blocking error** shown. The Publish gate is `validation.canPublish`, which is exactly what these errors feed. **G.2** — placement derives the rest: saving the stub through the editor wrote **`phase: 'financial-analytics'`, `topic: 't4-qm2'`** — both derived from the module (`t4-qm2` on the `analytics` track), no manual entry, verified in SQL after the in-app save. `topic` is written only for finance-with-module so accounting essays' hand-set topics (which drive `/accounting/consolidation/:topic`) are never clobbered. The decorative asterisk on Topic/Phase removed; for finance essays with a module the control is disabled with "Derived from the module's track." |

### B.3 substitute — manual consistency sweep (ui-audit unavailable)

Findings, including what was deliberately not fixed:

1. **Fixed:** `FsliDetail` hardcoded *"Updated 6 Sep 2025 · 6 min read"* on all 24 pages — now the row's real `updated_at`, fake read-time dropped (nothing measures FSLI reading time).
2. **Fixed:** the fabricated-data family (`Draft · Dika Gustiana` on the track index, cash prose on FSLI, 45 phantom books) — all now read from data or say honestly that nothing is there.
3. **Not fixed — recorded:** the FSLI admin banner is in Indonesian while every other admin surface is English. Owner-facing copy; the owner's language preference on admin chrome is theirs to set. One sentence to change if wanted.
4. **Not fixed — recorded:** `FsliHeroSection` shows the same stock Unsplash photo with "Photo: Unsplash" on all 24 FSLI pages — decorative filler of the same family as the placeholder prose, but removing it leaves the hero bare and there is no per-item image source yet. Needs a design decision, not a patch.
5. **Not fixed — recorded:** the track index byline puts read-time inline (`Published · author · 16 min read`) while the module page right-aligns it. Both are honest; the inconsistency is cosmetic.
6. **Checked clean:** pencil affordance is 28px visual in a 44px-comfortable row and admin-only; sidebar ToC text (13px) against `text-muted-foreground` passes contrast on both themes; the books upload card uses the dashed-border insert convention already used elsewhere.

## PR #11 session (2026-08-01) — writing an essay, and reading one (gates W1–W5)

Measured on the dev server (`127.0.0.1:5173`) against the live project, Playwright/Chromium,
Supabase traffic relayed through a local Node CORS relay (same transport constraint as the
Fable session above; TLS verification never disabled). These gates were run on branch
`claude/writing-reading-s1` BEFORE it was reconciled with the parallel Fable-session work
merged via PR #10 — the two sessions independently implemented the same mandate. The merge
commit records which implementation won per file; post-merge re-verification is recorded
below the W rows.

| # | Gate | State | Evidence |
|---|------|-------|----------|
| W1 | Save path: autosave re-run; trigger audited; takeaways scoped; topic/phase derived | **PASSED** | **(1) Autosave, re-run after the save path changed:** pasted into `/admin/writer/finance/sf-01-04`, hard reload with no manual save → recovery banner offered, Restore returned **3,612 words** including an edit made after the first backup. This also closes GATE 6's outstanding half. **(2) The trigger:** `validate_essay_tone_fields_trigger` was already dropped from live by `20260801125833` (applied by a parallel session that never committed the file — repo mirror now written, with the function body quoted). What it did: on `status='published'` it RAISEd unless the persona's `*_fields` was populated; on `status='draft'` it silently rewrote `NEW.status='tone_pending'` when a payload was filled or `voice_role='hybrid'`. After the persona columns dropped it became a hard error on every essay save (`record "new" has no field ...`). Verified gone: `pg_trigger` and `pg_proc` both empty for it. **The exact prior failure re-tested:** re-saved published `fa-07-01` through the editor (Publish click, "Published!") → row still `status='published', published=true`, 81 blocks / 21,946 chars / 5 refs / 3 takeaways. **(3) Takeaways policy, observed on real stubs:** `sf-01-03` set to `lesson_type='exercise'`, deck+800-word body filled, **0 takeaways → Publish enabled → "Published!"** landed as `status='published'` with `key_takeaways: []`; `sf-01-04` (`concept`), same state → **Publish disabled**. Half-filled (1–2) stays an error under every type. **(4) Derivation, observed on real stubs:** editing a module-placed essay derives everything from the ONE module field — both stubs saved with `phase='strategic-finance'`, `topic='t2-m01'`, `finance_section='strategic-finance'` (all NULL before); the Topic/Phase control renders read-only *"Derived from module: Strategic Finance · T2-M01"* for curriculum essays and lost its decorative asterisk elsewhere. Probe state fully reverted (stubs back to draft, bodies/decks cleared, revisions deleted); the derived placement fields stay, being correct. |
| W2 | Editor access: admin Edit affordance; empty stub opens + saves; non-admin and anon see nothing | **PASSED** | Measured by `gate23.mjs` in a real browser against the live DB, three identities on `/finance/strategic-finance` with the T2-M01 module accordion opened. **Admin** (`s7-admin@`, role granted service-side): **4** `a[aria-label^="Edit "]` pencil links present; draft titles href to the editor, not the public route — first two collected hrefs `/admin/writer/finance/sf-01-03`, `/admin/writer/finance/sf-01-04`; navigating the pencil URL for the *empty real stub* `sf-01-04` opened the TipTap editor (`.tiptap[contenteditable="true"]` present, no error boundary text) and **Save Draft → "Saved!"** — an empty stub both opens and saves. **Non-admin** (`s7-user@`, no role): **0** pencils, **0** `Draft` strings anywhere in the rendered page. **Anonymous:** **0** pencils, **0** draft strings, and at the API `GET /essays?published=eq.false` with the publishable key → `content-range: */0` — zero drafts exist for that identity, measured at PostgREST, not inferred from the UI. Harness note, recorded per mandate: the first three runs failed with the editor timing out — bisection showed login itself failing (`invalid_credentials` on direct curl) because a **parallel session had deleted the previous sweep accounts mid-run**; fixed by minting session-namespaced `s7-*` identities, after which the probe passed unmodified. |
| W3 | Reading path: every title a canonical link or deliberately not; labels real; mismatch redirects; route count reconciled | **PASSED** | **(1) Named title clicks, anonymous browser:** homepage → "Driver Tree Construction" landed `/finance/analytics/t4-m07/fa-07-01`; homepage → "Site Rebuild Note" landed `/essays/site-rebuild-note`; `/finance/analytics` track index → same essay landed canonical; `/finance/analytics/t4-m07` module page → same essay landed canonical. 4/4 clicks landed the expected path with >2,000 chars rendered. **(2) Link inventory.** *Now canonical links:* FinanceTrackIndex rows (were plain text at lines 36–49 — the defect that opened this section), FinanceModulePage rows, homepage cards, RelatedEssays and RelatedContent (both had local URL builders replaced with `essayUrl`), admin View-live anchors. *Deliberately NOT links, recorded:* FSLI detail pages inline the essay body under its title (the title is the page's own heading); WriterList titles open the editor (an admin authoring surface, not a reading one); About's essay section renders empty because `is_selected` is true on 0 rows (intentional empty state, verified in SQL at GATE 1a). **(3) Status labels are real data:** published `fa-07-01` row renders **"Published · Dika Gustiana · 16 min read"**; as admin, the four T2-M01 stubs render **"Draft · Dika Gustiana"** ×4 — replacing the hardcoded `Draft · Dika Gustiana` literal that previously printed for every essay regardless of status (FinanceTrackIndex.tsx:49). **(4) Mismatched URL canonicalises:** `/finance/wrongtrack/wrongmodule/fa-07-01` → redirected, landed `/finance/analytics/t4-m07/fa-07-01` (essay's own joined module is authoritative; an unplaced essay falls back to `/essays/:slug`). This closes the finding logged at GATE 1f. **(5) Route count reconciled** in `docs/ROUTE_VERIFICATION.md`: 68 loose grep hits (2 are `<Routes>`/`<RouteFallback`), **66** real `<Route path>` entries = **65 swept + the `*` catch-all**, which is deliberately excluded from the sweep and separately verified by GATE 1f's 404 observations. |
| W4 | Essay page: sticky ToC sidebar tracks to the final heading; 375px clean; reduced-motion jumps | **PASSED** | Measured by `gate4.mjs` on `/finance/analytics/t4-m07/fa-07-01` (the 13-entry essay), anonymous reader. **Where the code went, honestly:** the mandate targeted `LongformArticleShell`, but that shell is only selected for `track === 'finance-in-motion'` — a track slug no module has — so the essay the gate is observed on renders through `ArticleShell`. Both shells now share the same two-column pattern via an `ArticleToc` `variant="sidebar"`; the measurements below are from the shell readers actually hit. **(1) Sticky sidebar at 1280×800:** present and visible, 13 entries; the rail's top holds at **128px at scrollY 1000 and at 4000**. **(2) Tracks to the final heading, by scrolling:** stepping through the whole essay, **13 of 13 distinct entries** activated in turn and the entry active at the end is *"Least-confident assertions"* — the last ToC entry. Observer callback also fixed to pick the **topmost** intersecting heading (entries arrive in observation order, not document order). **(3) Internal overflow at 1280×520:** list `scrollHeight 580 > clientHeight 328`; as the reader scrolls the essay, the list scrolls **itself** from 0 to **252** to keep the active entry visible (`page never moved horizontally`, active entry within the list viewport). `max-h-[calc(100vh-12rem)]` + `overflow-y-auto`; the auto-scroll uses `list.scrollTo`, never `scrollIntoView`, which would drag the page. **(4) 375px:** `scrollWidth 375 == innerWidth 375`, sidebar hidden, inline collapsible present (`button[aria-expanded]`). **(5) Reduced motion:** with `reducedMotion: reduce` emulated, clicking a deep entry lands at the destination **by the first 50ms sample** (scrollY 10492 across all 12 samples — a jump); without it, the same click glides over ~600ms. **Root cause found on the way:** `html { scroll-behavior: smooth }` was unconditional, and `scrollIntoView({behavior:'auto'})` resolves to the *computed* scroll-behavior — so the first fix attempt still glided. The CSS is now gated behind `prefers-reduced-motion: no-preference` (fixing every anchor/programmatic scroll on the site, not just the ToC) and the click handler uses `'instant'`. **(6) ReadingProgress** rewritten: one rAF-coalesced `scrollHeight` read per frame, no React state per scroll event, bar animates `transform: scaleX()` (compositor-only) instead of `width` — observed at `scaleX(1)` at essay end. **Harness note:** two probe iterations failed because the probe itself scrolled too coarsely, and the third because `scrollBy` under smooth scroll-behavior retargets an in-flight glide (200×100px steps only reached y=5065 of 12803) — the debug run that proved it is recorded in `dbg4.mjs`. |
| W5 | Consistency: ui-audit run + dispositioned; FSLI pages honest; five deps gone; build passes | **PASSED** | **(1) ui-audit run**, scoped to the reading surfaces, findings recorded and every one dispositioned in `docs/UI_AUDIT_READING.md`: 12 findings (4 HIGH, 4 MEDIUM, 4 LOW), 11 fixed, 1 recorded-not-fixed with the reason, plus a deliberately-not-flagged list. Recon found zero `ease-in` / `scale(0)` / `h-screen` / pure-black on any reading surface — the defects were truth defects, not aesthetic ones. **(2) FSLI pages, observed on three line items** (`gate5.mjs`, anonymous, plus a follow-up settling check on the cold first page): `cash-and-cash-equivalents`, `inventories-net`, `trade-receivables-net` each render **10× "Not written yet."** for their unwritten sections, **0** occurrences of the cash boilerplate ("readily convertible to known amounts"), 0 stock-photo credits, 0 fake "Key points", 0 "6 min read", 0 "6 Sep 2025" — and each page now shows its OWN reported figures (2,614,318/5,018,739/3i.6 vs 809,397/658,712/3j,11 vs 2,203,018/2,063,070/3b,4dc) with the caption "Figures as reported in the source financial statements" and the real `Updated Jul 31, 2026`. `any_pair_identical: false` on full rendered text. 375px FSLI page: `scrollWidth 375 == innerWidth`. Seeding prose was deliberately NOT chosen: inventing accounting doctrine for 24 line items is the same fabrication class the section exists to remove (recorded in DECISIONS). **(3) The five @tiptap deps** (`blockquote`, `bullet-list`, `code-block`, `heading`, `ordered-list`): **0** occurrences in `package.json`, **0** imports in `src/` — they were already removed as direct deps by `c7e5d12` (prior session, merged via PR #9); the remaining lockfile entries are **transitive dependencies of `@tiptap/starter-kit` itself**, which is exactly why declaring them directly was redundant and also why they cannot leave the lockfile. `npm run typecheck` (249 files) and `vite build` both pass after the Section-5 batch. |

### Post-merge re-verification (PR #11 reconciled with `main` @ `8201dc4`)

Two sessions implemented this mandate independently; the merge keeps ONE implementation per
feature (see the merge commit for the per-file matrix). Every claim above was measured on a
pre-merge tree, so the reading path was re-measured on the merged tree — same probes, cold
dev server:

- **W4 re-run, unchanged:** sidebar present with 13 entries, sticky top holds at **128px**
  across a 3,000px scroll, **13/13** entries activate in document order ending on
  *"Least-confident assertions"*, list self-scrolls **0 → 252px** while the page stays put,
  375px `scrollWidth == 375`, reduced-motion click lands by the first 50 ms sample while the
  normal-motion click glides. Progress bar reaches `scaleX(1)`.
- **W5 re-run, unchanged:** three line items each render **10× "Not written yet."**, zero
  cash boilerplate, zero Unsplash credit, zero fake key points, real `Updated Jul 31, 2026`,
  and their own figures (2,614,318 / 809,397 / 2,203,018). `any_pair_identical: false`.
- **W3 re-run on the merged (main-side) link implementation:** 4/4 named title clicks land
  canonical, `/finance/wrongtrack/wrongmodule/fa-07-01` redirects to
  `/finance/analytics/t4-m07/fa-07-01`, published label reads
  *"Published · Dika Gustiana · 16 min read"*, and anonymously: **0** pencils, **0** draft
  strings, `GET /essays?published=eq.false` → `content-range: */0`.
- **Review finding fixed and locked down.** A reviewer noted the Key Takeaways card promised
  a per-lesson-type relaxation in contexts where `WriterEditor` sends `lessonType: null` and
  the gate stays strict — editorial essays and unplaced finance essays were told the opposite
  of what Publish would do. The copy is now conditional on a module-placed finance lesson, and
  `tests/unit/takeawaysGuidance.test.tsx` (8 assertions) pins gate and guidance together,
  including the fail-closed behaviour for an unknown lesson type.
- `npm run typecheck` (tsc -b, 249 files), `vite build`, and **171 unit tests in 17 files**
  all pass on the merged tree.
- **Not re-run post-merge, recorded honestly:** the browser admin/non-admin identity pass
  (W2). GoTrue returned `over_email_send_rate_limit` for new signups and the session's own
  namespaced identities had already been deleted, so no admin session could be established.
  The merged admin surfaces are `main`'s own implementation (shared `EssayEditLink`), already
  gate-verified there across three identities, plus two changes from this branch: the edit
  link's hit target widened to 44px, and the takeaways guidance copy, which the new unit test
  covers. The anonymous half of W2 **was** re-measured (see above).

## Writing-surface session (2026-08-01) — gates S1–S8

Measured on `vite build` + `vite preview` (`127.0.0.1:4173`) against the live project
`asypkbkiebjvvpimewfp`. Chromium in this container cannot complete TLS to `*.supabase.co`,
so Supabase requests are intercepted by Playwright and fulfilled from Node
(`scratchpad/supabaseRoute.mjs`) — same method, headers, body and real responses, only the
transport hop differs. TLS verification was never disabled.

| # | Gate | State | Evidence |
|---|------|-------|----------|
| S1 | Canvas: title first, measure set explicitly, chrome stripped, 375px clean | **PASSED** | `/admin/writer/finance/ff-01-01` as admin. First focusable element in the canvas is the **Title** textarea (`aria-label="Title"`), 48px from the top of the scroller; the first editable body line sits at **339px** — the old layout spent ~130px on toolbar and border before the title even appeared, and there is now nothing between them but the title and subtitle themselves. Placeholders read **"Title"** and **"Add a subtitle..."**. Body paragraph computed: `max-width` **680px**, `font-size` **18px**, `line-height` **31.5px** (1.75) — all three set explicitly in `.essay-prose`, none inherited. Bar text is exactly `Posts Draft · Saved Preview Continue`. Live word count **absent**, read time **absent**, validation text **absent**. At 375px `scrollWidth == clientWidth == 375`, before and after autosave, with and without the insert menu open. |
| S2 | Formatting reachable through one surface only; all eight actions survive publish | **PASSED** | With no selection the only titled button on the page is **`Insert block`** — the persistent toolbar is gone, not hidden. On a text selection the bubble menu offers exactly eight: `Bold (Ctrl+B)`, `Italic (Ctrl+I)`, `Strikethrough`, `Link`, `Heading`, `Subheading`, `Quote`, `List`. All eight applied to eight marked lines in a real stub through that menu (8/8 clicks landed), producing exactly one element each: `strong 1, em 1, s 1, a 1, h2 1, h3 1, blockquote 1, ul 1`. Saved, published, read **anonymously**: bold ✓ italic ✓ strikethrough ✓ link ✓ (`-> https://example.com/gate2-link`) H2 ✓ H3 ✓ quote ✓ list ✓. `fa-07-01` unchanged — `content` md5 `b36b8ba5…` identical before and after the session; **81** `content_json` blocks, **80** rendered (one is an empty paragraph, which `ArticleBody` drops by design — the 81/80 difference is accounted for, not a loss). |
| S2b | Strikethrough silently stopped rendering on publish | **FOUND AND FIXED** | The first run of S2 returned `strikethrough: null` while the word `STRIKELINE` was present — text surviving, formatting not. Cause: published pages pass `essays.content` (the HTML column) to `ArticleBody`, so the *legacy HTML* branch is the live path, not a fallback — and it named `strong`, `em`, `code` but not `s`/`strike`/`del`, `u`, `sup`, `sub` or `br`. Unnamed tags fall through to `processChildren`, which returns the children unwrapped. Fixed and covered by `tests/unit/articleBodyInline.test.tsx` (12 cases). Re-measured: strikethrough now reads back from the published page. |
| S3 | One menu, two entry points; insertions only; every item through all four contract places | **PASSED** | Gutter `+` and `/` are the same list from `src/lib/tiptap/insertMenu.ts`: both return `["Image","Link preview","Table","Code block","Divider"]`, string-equal. **No formatting items** — H2, H3, both lists and Quote moved to the bubble menu. Keyboard: typing `tab` filters to `["Table"]`; arrows move; Enter picks; **Escape closes and leaves the `/` in the document** (`menuGone: true, slashSurvivedInDoc: true`). Menu opened on a line 765px down a 900px viewport flips above the caret — `top 405, bottom 678`, `clippedBelow false`, `clippedAbove false`, 5 items visible. At 375px: `left 16, right 271`, not clipped, and `document.scrollWidth == 375`. Each item inserted through the real UI (image through the uploader's real upload path, link preview through its dialog), then **saved → hard-reloaded → published → read anonymously**: figure ✓ (`src` a real storage URL, `alt "Gate probe image"`, caption "A one-pixel gate probe."), link preview ✓ (href + title intact), table ✓ (9 cells), code block ✓, divider ✓. All five are **top-level** blocks in `content_json`, not nested. |
| S3b | Two nodes were destroyed by a save/reload round trip | **FOUND AND FIXED** | Found by measuring `content_json` after S3, not by reading code: the row's JSON stopped at the table while its HTML kept growing — one row holding two different documents. Two causes. **(a)** `figure` and `linkCard` wrote their `data-*` JSON pre-escaped (`.replace(/"/g,'&quot;')`) into DOM that escapes attribute values again, so `JSON.parse` threw on every reload and every attribute fell back to its default — a figure reloaded as `src=""`. **(b)** `linkCard` renders as an `<a>`, which is exactly what StarterKit's Link mark claims, and ProseMirror gathers every *mark* rule before any *node* rule — so at equal priority the mark won and the card came back as an ordinary styled link. Fixed by `src/lib/tiptap/attrJson.ts` (escape once, at the layer that needs it; tolerant read for rows already written the old way) and `priority: 100` on the card's parse rule. `EssayEditor`'s HTML mirror now ignores its own echo instead of re-parsing it. Covered by `tests/unit/editorHtmlRoundTrip.test.ts` (9 cases). |
| S3c | Non-admin upload through the Image item | **PASSED** | Two levels. **Client:** an anonymous visitor at `/admin/writer/finance/ff-01-01` lands on `/auth` — no `.ProseMirror`, **0** `Insert block` buttons, **0** file inputs, so the Image item is unreachable. **Server:** the real RLS policy exercised directly — `set local role authenticated` with a non-admin `sub` claim, then `set local role anon`, each attempting `insert into storage.objects (bucket_id 'essay-images')`. Both refused; **0** probe rows written (`public_buckets_admin_insert` requires an `admin` row in `user_roles`). |
| S4 | Validation quiet while drafting; publish end to end from the modal | **PASSED** | An untouched empty stub (`ff-03-01`) shows **no** validation text, **no** word count, **no** read time; bar reads `Posts Draft · Saved Preview Continue`. Clicking **Continue** opens `Post settings`, which states exactly what is missing — `At least 3 key takeaways required (0/3)` — with **Publish now disabled**. Filling three takeaways clears the list and enables the button. Published from the modal; bar changed to `Posts Published · Saved`. Placement set in the modal reached the database: `module_id 3e00db3a…`, `finance_order 7`, `finance_section analytics`, `lesson_type concept`, and the derived `phase financial-analytics` / `topic t4-qm1`. Read **anonymously** at the canonical URL `/finance/analytics/t4-qm1/ff-01-01` — title, body and key takeaways all rendered, no 404. (Fixed along the way: the modal's Track select initialised once, before the module list had loaded, so it read "All tracks" every time and offered four different modules called "01 · …".) |
| S5 | Autosave says different things, and never overstates | **PASSED** | **Unpublished stub, 524 words typed, no manual save**: chip went `Saving…` → `Saved 05:51 PM`; hard reload returned **523 words** with the marker present and the title persisted, and the `essays` row itself holds **535 words** — the text is where the reader would find it, not only in a backup. No recovery banner, correctly. **Published essay `fa-07-01`, edited, no manual save**: chip read `Published · Backed up 06:42 PM` — never "Saved"; after a hard reload the marker was **absent from the editor** (the live row untouched) and the **recovery banner appeared**, so the text is in `essay_revisions` and offered rather than applied. **3,000-word test**: 3,007 words pasted into an unpublished stub, hard reload with no manual save — **3,007 words recovered**, exactly. |
| S6 | Paste from Word | **PASSED (synthetic fixture)** | No `.docx` exists in the repo, so the fixture is Word 365's clipboard *markup* — Office namespaces, `<!--[if gte mso 9]>` blocks, `MsoNormal` paragraphs with inline `mso-*` styles, `MsoListParagraphCxSp*` fake lists carrying `mso-list` and a literal bullet glyph, `<o:p>` filler, `<b>`/`<i>`, and an `<h1>` — wrapped around real `fa-07-01` prose. **21 source blocks → 18 blocks**, accounted for item by item: 1 `h1` → `h2` (the body schema has no `h1`; that is the essay title), 1 `h2` → `h2`, 1 `h3` → `h3`, 12 prose paragraphs → 12 paragraphs, 1 Word spacer paragraph → **dropped** (deliberate), 3 bullet paragraphs → **1 `<ul>` of 3 `<li>`**, 3 numbered paragraphs → **1 `<ol>` of 3 `<li>`**, plus 1 trailing empty paragraph the editor keeps for the caret. All 21 content items present. Bold, italic and the link survived with the href intact. `style` attributes remaining: **0**. Office classes remaining: **0**. `<o:p>` remaining: **0**. Stray bullet glyphs: **none**. Covered by `tests/unit/pasteFromWord.test.ts` (9 cases). **Two bugs found by this gate:** attribute stripping ran *before* list detection, so every test for `mso-list`/`MsoListParagraph` read an already-emptied attribute; and the block walk iterated `body.children`, which for Word clipboard HTML is the single `<div class=WordSection1>` wrapper. Both fixed. **Not verified:** the owner's actual `.docx` — the gate should be re-run against one. |
| S7 | Reader side matches the specification | **PASSED** | Anonymous. Header children in order: `DIV: DIKA GUSTIANA` (masthead, with rule), `H1: <title>`, `P: <subtitle>`, `DIV: <topic · author · date · read time>` (with rule) — then the body. Masthead is first (`mastheadFirst: true`). Body paragraph: width **680px**, `max-width` **680px**, `font-size` **18px**, `line-height` **31.5px**. A figure renders **768px** against the 680px text column — wider, as specified (`max-width: min(780px, 100vw - 2rem)`). Reaction row `♡ 💬 ↗`: **absent**, deliberately not built. No horizontal scroll at 1440 or at 375 (`scrollWidth == clientWidth == 375`). |
| S8 | `import-admin` gone | **PASSED** | `auth.users` now lists exactly one account: `dika.g.irawan@gmail.com`, confirmed, 1 identity, role `admin`, last signed in 2026-08-01 16:36. `import-admin@dikagustiana.com` and its `user_roles` row are deleted. **8.2 closed:** the admin browser identity pass was BLOCKED on there being no usable admin identity; there is one now, confirmed and used. **8.3 remains BLOCKED:** the git proxy still rejects delete-pushes; the eight zero-unique-commit branches need the GitHub UI. `archive/pre-rebuild-history` untouched. |

### Session hygiene

Everything the gates created was removed afterwards, and the removal was measured:
the temporary published probe essay is deleted (`0` rows matching `zz-gate%`); the four
stubs `ff-01-01`, `ff-02-01`, `ff-02-02`, `ff-03-01` are back to their curriculum-seed
titles, snippets, module placement and order with `content`/`content_json` null and
`published false`; their gate-run revisions are deleted (`0` left); the eight one-pixel
probe images are removed from the `essay-images` bucket through the Storage API (the two
pre-existing images are untouched); `fa-07-01`'s body is byte-identical to its
pre-session md5. **0** public tables without an RLS policy.

## Notes

### `tsc --noEmit` was checking ZERO files (found 2026-08-01, fixed)

Worth stating loudly, because every "typecheck passes" in this ledger before this
entry was vacuous. The root `tsconfig.json` is solution-style — `"files": []` with only
`references` — so plain `tsc --noEmit` compiles nothing and exits 0. Proven with
`--listFiles`: **0** project files under the old command, **249** under
`tsc -p tsconfig.app.json`.

It surfaced because a JSX syntax error I introduced in `Breadcrumb.tsx` (a `{/* comment */}`
placed as a sibling of the root element inside `return (`) took the entire homepage down with
a 500 — while `npm run typecheck` reported clean. Reintroducing the error confirmed it:
`tsc --noEmit` exit code **0** with the file broken.

`typecheck` is now `tsc -b --force`, which actually builds the referenced projects. Running it
for the first time surfaced two real errors that had been hiding:
`LivePreviewPanel` reached for `.label`/`.url` on `PresentationReference`, which is
`string | {label, url?}` (legacy rows stored bare strings), and `WriterEditor` passed a `Json`
where `EssayPresentation` was expected. Both fixed; a shared `normaliseReference` helper now
handles the string form.

**This is the gate philosophy proving itself.** A green typecheck was one of the five things
the mandate says can never pass a gate, and here it was not merely insufficient — it was
measuring nothing at all.

**Section 0 housekeeping.** `@tiptap/html` confirmed absent from both `package.json` and
`package-lock.json` (0 occurrences in each) — the earlier ambiguity is closed.
